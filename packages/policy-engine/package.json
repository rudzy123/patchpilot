{
  "name": "@patchpilot/policy-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-independent policy-engine boundary. No scoring in this iteration.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:unit": "vitest run --config vitest.config.ts",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@patchpilot/eslint-config": "workspace:*",
    "@patchpilot/typescript-config": "workspace:*",
    "@types/node": "^26.4.0",
    "eslint": "^9.39.1",
    "typescript": "~7.0.2",
    "vitest": "^4.1.11"
  }
}
