import type { ReactElement } from 'react';

import { SessionExpiredPageClient } from './session-expired-page-client';

export default function SessionExpiredPage(): ReactElement {
  return <SessionExpiredPageClient />;
}
