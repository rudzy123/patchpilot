# Public web configuration only. Copy to `.env.local` if you need to override the default.
# Do not put server secrets, database URLs, or object-storage credentials in this file.
NEXT_PUBLIC_PATCHPILOT_ENVIRONMENT=development
